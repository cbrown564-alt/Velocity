{
  "name": "@velocity/mcp-server",
  "private": true,
  "version": "1.0.0",
  "description": "MCP server transport adapter for Velocity engine",
  "type": "module",
  "main": "./dist/index.js",
  "bin": {
    "velocity-mcp": "./dist/index.js"
  },
  "scripts": {
    "build": "node -e \"console.log('@velocity/mcp-server: use npm run mcp:build from repo root (vitest)')\"",
    "dev": "tsx index.ts"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "1.0.4"
  }
}
