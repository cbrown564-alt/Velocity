/** Max simultaneous toasts — keeps first-run from feeling noisy */
export const MAX_VISIBLE_TOASTS = 2;

/** Defer backup reminder until onboarding crosstab + story shelf settle */
export const STORAGE_REMINDER_DELAY_MS = 15_000;
